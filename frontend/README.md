# TEAjudo — frontend

Interface React (painel de botões, jogos, área dos pais). Veja o `README.md`
e o `CLAUDE.md` na raiz do repositório para o contexto completo do projeto
(frontend + backend).

```bash
npm install
cp .env.example .env
npm run dev
```
